version: "2"
services:
  haproxy:
    #image: eeacms/haproxy
    image: h30
    depends_on:
    - webapp
    ports:
    - "80:8080"
    - "1936:1936"
    environment:
      FRONTEND_PORT: "8080"
      BACKENDS: "webapp"
      BACKENDS_PORT: "8080"
      DNS_ENABLED: "True"
      HTTPCHK: "meth GET uri /"
      INTER: "5s"
      LOG_LEVEL: "info"
  webapp:
    image: eeacms/hello
    scale: 4
    environment:
      PORT: "8080"
